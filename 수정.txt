프로젝트 생성할때 중복 이름 경고창 위치 변경

이미지 사이즈 디폴트를 224, 

데이터셋, 전처리 같이

데이터 전처리 할때
총 파일의 개수 = 이미지 파일 + 라벨링 파일
모든 파일에 대해서 파싱을 수행할 경우 (예 10만장)
20만개의 파일에 대해서 파싱을 해야하니 시간이 너무 걸림
따라서, 클래스 별로 개수를 센 뒤 파일의 개수가 안 맞는 클래스에서만 짝이 없는 파일 찾기를 수행
표시)  전체 데이터셋 : xx장, 클래스별로도 xx장이 뜨게끔


if 어떠한 클래스에서 짝이 안맞는다?:
	그 클래스에 대해서만 검증 수행 / 사용자에게 검증 수행할꺼냐 묻고 예 누르면 검증 수행 (------------- 1000/1000"데이터 검증 수행중입니다 : 데이터 짝 안맞음")
	그 뒤 짝이 안맞는 파일은 드롭
else: 그냥 진행

스플릿도 여기서 지정할 수 있게끔
정규화 빼 (물어보고)

증강 부분
블러, 노이즈, 랜덤 크롭 물어보고 빼기
로테이션 추가

결과창에
2x2 그래프로
train acc, train loss
val acc, val loss

로그 올리기
로그도 중첩되게 20%나오게 x tqdm써서 받아오기

당연히 모델의 성능은 val이 아닌 test set에서만 수행
지금 confusion matrix가 yolo는 제공이 되지만
다른 분류 모델에서는 제공 x 따라서, confusion matrix를 만드는 코드를 짜서
모든 모델이 하나의 템플릿의 cm에 맞게 나오게끔 수행
단, test split로 띄우기

튜닝 제안 란 빼버리기

지금 성능 지표 나오는거
train acc top1, train loss
val acc, val loss, val precision, val recall, val f1-score (뭐 된다면 macro로)
테스트 셋으로 최종 성능 도출할때도 마찬가지 검증 셋이랑 똑같이 나오게



실시간 로그, 그래프(추가)